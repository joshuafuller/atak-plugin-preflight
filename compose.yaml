services:
  preflight:
    build: .
    image: atak-plugin-preflight:local
    working_dir: /workspace/source
    volumes:
      - ${ATAK_PLUGIN_SOURCE:-.}:/workspace/source:ro
      - ${ATAK_PREFLIGHT_REPORTS:-./reports}:/workspace/reports
    command: ["/workspace/source"]
